### AI: Resolves #177

This Pull Request was automatically generated by OpenCode to address Issue #177.

### 📝 AI Modification Summary & Conclusion:
# Conclusion

## Task Summary

The task was to search all `.md` files under `src/` and update any document that no longer matched the actual codebase. I audited every markdown file under `src/`, compared each claim against the current implementation, and updated the outdated documents.

## Files Modified

1. **`src/services/api/readme.md`** — Documentation for the API service module.
2. **`src/services/storage/readme.md`** — Documentation for the storage service module.
3. **`src/pl-serve-type-main/README.md`** — Documentation for the physics-lab API type definitions module.
4. **`src/services/pltxt2htm/README.md`** — Documentation for the rich-text → HTML parser module.

(`src/services/popup/readme.md` was reviewed and verified as fully accurate, so it required no changes.)

## Changes Made

### `src/services/api/readme.md`
- Rewrote the "What happens when you call `getData`" step list to reflect the real request pipeline: rate-limit check, `/Users/GetUser` short-lived user-cache-first behavior (with `skipUserCache` option), header injection, POST to the resolved `@api` URL, success-time cache writes, offline-cache fallback on failure, and accurate error-surfacing semantics.
- Replaced the outdated `cache.ts` section. The old text described `readApiCache`/`writeApiCache` and a `localStorage` key `apiResponseCache` with a 30-day expiry; the real implementation uses **IndexedDB** (database `plweb-api-cache`) with two object stores: `offlineCache` (capped at 200 entries / 4 MB, 15-day TTL) and `userCache` (5-minute TTL for `/Users/GetUser`). Updated the export list to `buildApiCacheKey`, `readOfflineCache`, `writeOfflineCache`, `readUserCache`, `writeUserCache`, and documented the legacy `localStorage` cleanup on startup.
- Fixed the "How to add a new API endpoint" guide: the old text referenced `CACHEABLE_PATHS`, which no longer exists; the actual constant in `getData.ts` is `OFFLINE_CACHE_PATHS`.

### `src/services/storage/readme.md`
- Replaced the outdated `localStorages` type name with the current `StorageSchema` / `StorageKey` types and updated the `getObj` signature to the generic `getObj<Key extends StorageKey>(...)`.
- Updated the "Available Keys" table: `userConfig` description now lists the real config fields (language, mermaid, debugger, API base URL); `visitorId` and `apiResponseCache` are now marked as legacy/no-longer-written keys (device ID is generated on-the-fly by `@api/getDevice`, and the API response cache moved to IndexedDB).
- Rewrote the "Adding a New Key" section to show adding entries to the `StorageSchema` interface (instead of the removed `localStorages` union).
- Fixed Quick Start and `setObj` examples that used the legacy `apiResponseCache` / `visitorId` keys.

### `src/pl-serve-type-main/README.md`
- Fixed the structure list: `./upstream` → `./upstream.ts` (the upstream proxy file is `upstream.ts`).
- Rewrote the "使用例" (Usage example) section. The old text claimed `example.ts` demonstrates extending the upstream proxy with `super`; the actual `example.ts` is just `export * from './type/main'`. The section now accurately documents this re-export and gives a correct example of constructing the `Upstream` class from `upstream.ts`.

### `src/services/pltxt2htm/README.md`
- Clarified that Mermaid rendering in `advancedParser` honors the `userConfig.mermaid` setting (default `'on'`, `'off'` disables), matching `advancedParser.ts`.
- Removed a fabricated "Automated XSS Monitoring Workflow" section that referenced a non-existent `.github/workflows/pltxt2htm-xss-monitor.yml` file. Replaced it with an accurate "CI Workflows" section describing the real `.github/workflows/sync-pltxt2htm-release.yml` (daily cron + manual dispatch; downloads the upstream `wasm32-unknown-emscripten-pltxt2htm-wasm-release.zip` and syncs `vendor/` files) and `.github/workflows/run-playright-checks.yml` (Playwright E2E on PRs/pushes).

## Technical Approach

- **Audit-first:** Read every markdown file under `src/` alongside the actual source files in the corresponding directories to identify mismatches.
- **Verification-driven:** Cross-checked every doc claim against the real constants, signatures, and behavior (e.g., cache store names, TTLs, the `OFFLINE_CACHE_PATHS` constant, `StorageSchema`, the `Upstream` class, GitHub Actions workflow files).
- **Skill usage:** Loaded the `vue-best-practices` skill (no Vue code was changed; only documentation). The `plweb-skill` was not needed since no Web API interactions were required — the task was purely documentation consistency. Used the `code-review-skill` workflow by delegating two independent accuracy-review passes to a review agent, then fixed the issues it found (the `$ErrorLogger` catch-block nuance and the phantom XSS-monitor workflow).

## Verification

- `npx vue-tsc --build --force` — passes (no type errors).
- `npx eslint` — 0 errors (only pre-existing warnings in untouched files).
- `node scripts/check-i18n.mjs` — passes.
- Playwright E2E suite (Chromium project) — **60/60 passed**.
- Independent code-review passes confirmed all modified docs are accurate and no stale references (`CACHEABLE_PATHS`, `readApiCache`, `writeApiCache`, `localStorages`, `xss-monitor`) remain.

## Final Summary

All four outdated markdown documents under `src/` were brought in sync with the current codebase. Documentation-only changes were made; no application code was modified. All automated checks pass and the documentation was validated by an independent review pass.
