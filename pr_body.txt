### AI: Resolves #178

This Pull Request was automatically generated by OpenCode to address Issue #178.

### 📝 AI Modification Summary & Conclusion:
# Conclusion

## Task
Internationalize all hard-coded (non-i18n) user-visible text across the plweb2 Vue 3 codebase.

## Changes

### i18n locale files (`src/i18n/{zh,en,de,ja,fr}.ts`)
All five locales were updated in lockstep so that `scripts/check-i18n.mjs` stays green:
- `comments.title` — parameterized comment title (`{name}` / `{category}`)
- `errors.requestTimeout` — request-timeout message
- `expeSummary.wordCount` — now parameterized with `{count}`
- `mdEditor.save` — "Save" action label (was missing for the `t('mdEditor.save')` call in `Editor.vue`)
- `about.*` — slogan (slogan1..3) and typing-effect phrases (type1..8)
- `ui.messages.loginRequiredFirst` / `ui.messages.openLinkFailed` — toast messages

### Components / views / services
- `src/views/Comments.vue` — replaced hard-coded `的` title template with `t('comments.title', { name, category })`
- `src/views/Notifications.vue` — replaced hard-coded Chinese `n-tab-pane` `name` values (全部/系统消息/回复和评论/关注和粉丝/作品/管理通知) with stable ASCII identifiers
- `src/views/Editor.vue` — replaced hard-coded `Discussion`/`Experiment` tab labels with `t('worklist.categoryDiscussion'|'categoryExperiment')`
- `src/views/ExperimentSummary.vue` — removed hard-coded Chinese colon; word count now uses `t('expeSummary.wordCount', { count })`
- `src/views/About.vue` — slogan and typing-effect phrases now load via i18n (`about.*`); the hard-coded typing queue was rebuilt dynamically from keys
- `src/components/messages/MessageList.vue` — fixed invalid key `error.unknownError` → `errors.unknownError`
- `src/components/popup/ApiErrorDialog.vue` — default `OK`/`Cancel`/`Retrying...` labels now fall back to existing `ui.ok`/`ui.cancel`/`ui.retrying` translations
- `src/services/getUserCurentAvatarByID.ts` — console-only timeout error message normalized to English
- `src/main.ts` — `rendering...` placeholder now uses `ui.messages.loading`; `Please login first` / `Failed to open link` toasts now use i18n; added `document.documentElement.lang` sync on locale change

### Deliberately left unchanged
- `Tags: ['精选']` and `Tags/精选` in `wortList.vue` / `Profile.vue` — server-side API protocol values, not UI text; translating would break requests
- `NotificationList.vue` multilingual test template — mirrors the server `MessageTemplate` data shape (has proper per-language fields)
- `Footer.vue` commented-out `<span>我的</span>` — not rendered
- Internal diagnostic messages sent to the error logger (e.g. PWA check failures) — not user-facing

## Verification
- `npm run eslint` — 0 errors (16 pre-existing complexity warnings) and `i18n check passed: all locale keys are aligned and translated`
- `npm run build` — `vue-tsc` type check + `vite build` both succeed
- No test script is defined in this project
